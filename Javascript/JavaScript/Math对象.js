// Math.ceil(number); 把浮点数number往"大于"方向取最接近的整数
Math.ceil(3.14); // 输出4
Math.ceil(3.98); // 输出4

// Math.floor(number); 把浮点数number往"小于"方向取最接近的整数
Math.floor(3.14); // 输出3
Math.floor(3.98); // 输出3

// Math.round(number); 把浮点数number"四舍五入"取最接近的整数
Math.round(3.14); // 输出3
Math.round(3.98); // 输出4